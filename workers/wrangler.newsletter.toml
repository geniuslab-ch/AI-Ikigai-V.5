name = "newsletter-subscribe"
main = "newsletter-subscribe.js"
compatibility_date = "2026-01-08"

# Environment variables (secrets must be set via CLI or dashboard)
# To set the API key: npx wrangler secret put BREVO_API_KEY --config wrangler.newsletter.toml

[observability]
enabled = true

[observability.logs]
enabled = true
